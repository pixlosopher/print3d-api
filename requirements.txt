# Core dependencies for Railway deploy
flask>=3.0.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
stripe>=7.0.0
google-generativeai>=0.5.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.32.5
httpx>=0.25.0
gunicorn>=21.0.0

# Optional: for mesh processing (comment out if not needed)
# trimesh>=4.11.1
# numpy>=1.24.0
